# minigames-export/ — Hinweis

Die neun Spiel-Beschreibungen in diesem Ordner liegen **byte-gleich** auch im
Repo `Fauteck/vibecoding-academy` unter `projekte/`. Beide Kopien bleiben
bewusst bestehen (Entscheidung 2026-09-25); kein Gate hält sie gleich. Wer eine
davon ändert, zieht die andere nach.
